{
  "_readme": "Copy to keys.json (gitignored). Map each serverId (from src/data/servers.ts) to that server's ss:// access key. Any server not listed here falls back to OQUS_ACCESS_KEY in .env. To add a server: deploy a VM, get its ss:// key, add it below, restart the backend.",
  "ca-tor": "ss://BASE64_OF_method:password@1.2.3.4:8388",
  "us-ny": "ss://BASE64_OF_method:password@5.6.7.8:8388"
}
